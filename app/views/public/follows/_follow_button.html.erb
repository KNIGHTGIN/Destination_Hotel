<% if current_user.followings?(user) %>
  <%= link_to "フォロー外す", user_follow_path(user_id: user.id), method: :delete %>
<% else %>
  <%= link_to "フォローする", user_follows_path(user_id: user.id), method: :post %>
<% end %>